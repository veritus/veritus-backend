version: '3'

services:
  db:
    image: postgres:latest

  backend:
    build: .
    environment:
    - DB_USER=postgres
    - DB_PASS=pass
    command: python3 src/manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db